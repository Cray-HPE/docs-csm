name: Backport

on:
  pull_request:
    types:
      - closed
      - labeled

jobs:
  backport:
    runs-on: self-hosted
    name: Backport closed pull request
    steps:
    - uses: syndesisio/backport-action@master
